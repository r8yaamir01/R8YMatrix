# Netlify redirects file

# Redirect domain aliases to primary domain
https://www.r8ymatrix.netlify.app/* https://r8ymatrix.netlify.app/:splat 301!

# Optional: Redirect old file paths if needed
# /old-path /new-path 301

# Handle 404s with the custom page
/* /index.html 200 